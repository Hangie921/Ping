p=companies
each company in companies
	- var c = company.toObject()
	ul
		li 
			a(href='/companies/'+c.username) #{c.username}
		li 
			a(href='/api/contact/'+c.username) /api/contact/#{c.username}
		each v,k in c
			li <strong>#{k}:</strong> #{JSON.stringify(v)}
